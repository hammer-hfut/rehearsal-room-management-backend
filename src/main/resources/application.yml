quarkus:
  datasource:
    devservices:
      init-script-path: "/dev/init.sql"
  http:
    port: 16458
    enable-compression: true
  cache:
    caffeine:
      "maij-token-policy":
        maximum-size: 100
